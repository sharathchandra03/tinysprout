node_modules/

# Local secrets — never commit. (.env.example IS committed.)
.env
.env.local

# Legacy local storage from the old SQLite/disk version (no longer used).
data/
uploads/
*.db
*.db-shm
*.db-wal

# OS junk
.DS_Store
